<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="AgendamentoCarro.DetalheViewPage" Title="{Binding veiculo.Nome}">
    <ContentPage.Content>
        <StackLayout Padding="25">
            <!-- <Label Text="Esta é a página de detalhe" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" /> -->
            <TableView Intent="Settings">
                <TableRoot>
                    <TableSection Title="Acessórios">
                        <SwitchCell Text="{Binding TextoFreioAbs}" On="{Binding TemFreioAbs, Mode=TwoWay}" />
                        <SwitchCell Text="{Binding TextoArCondicionado}" On="{Binding TemArCondicionado, Mode=TwoWay}" />
                        <SwitchCell Text="{Binding TextoMP3Player}" On="{Binding TemMP3Player, Mode=TwoWay}" />
                        <TextCell Text="{Binding TextoValorTotal, Mode=TwoWay}" />
                    </TableSection>
                </TableRoot>
            </TableView>
            <Button x:Name="buttonProximo" Text="Próximo" Clicked="Proximo_Clicked" VerticalOptions="End" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>